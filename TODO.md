# TODO - Corrections CLI

- [x] Plan approuvé
- [x] 1. Corriger `_print_menu()` — Afficher les options du menu
- [x] 2. Ajouter la question du statut dans `action_update()`
- [x] 3. Modifier `action_list()` pour afficher toutes les infos (via `to_cli_detail`)
- [x] 4. Corriger `action_get()` — conversion status/student_class en énumérations
- [x] 5. Tester l'application — **TOUS LES TESTS PASSENT**

